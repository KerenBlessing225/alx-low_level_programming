#include <stdio.h>
#include "main.h"

/**
 * main - a program that prints its name
 * @argc: the number of arguments typed in the command line
 * @argv: array that points to the arguments
 * Return: 0
 */
int main (int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
